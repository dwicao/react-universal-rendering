import React from 'react';
import { render } from 'react-dom';
import WebContainers from './containers';

const rootElement = document.getElementById('root');

render( <WebContainers />, rootElement );
